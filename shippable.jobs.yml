jobs:
  - name: promoteToProduction
    type: runSh
    steps:
      - IN: landing-pages_runCI
        switch: off
      - IN: landing-pages_gitRepo
        switch: off
      - TASK:
        - script: echo $STAGING_BUCKET
        - script: SOURCE=$STAGING_BUCKET DEST_BUCKET=$PRODUCTION_BUCKET ./scripts/copy.sh
